```
<!-- sudo snap install go --classic -->
mkdir -p "$HOME/.local" && rm -rf "$HOME/.local/go" && cd /tmp && curl -fL --connect-timeout 15 --max-time 180 -o go1.22.6.linux-amd64.tar.gz https://dl.google.com/go/go1.22.6.linux-amd64.tar.gz && tar -C "$HOME/.local" -xzf go1.22.6.linux-amd64.tar.gz && rg -n "^export PATH=\$HOME/.local/go/bin:\$PATH$" "$HOME/.bashrc" || echo 'export PATH=$HOME/.local/go/bin:$PATH' >> "$HOME/.bashrc" && export PATH="$HOME/.local/go/bin:$PATH" && hash -r && go version && which -a go

curl https://sh.rustup.rs -sSf | sh -s -- -y && source "$HOME/.cargo/env" && cargo --version && rustc --version && export PATH="$HOME/.local/go/bin:$PATH" && export GOPROXY=https://goproxy.cn,direct && export GOSUMDB=sum.golang.google.cn 

uv sync --all-extras
```

sudo apt install pandoc
pip install pypandoc pymupdf