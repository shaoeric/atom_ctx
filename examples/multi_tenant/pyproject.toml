[project]
name = "multi-tenant-example"
version = "0.1.0"
description = "AtomCtx Multi-Tenant admin workflow example"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "atom_ctx>=0.1.6",
    "httpx>=0.27.0",
]

[tool.uv.sources]
atom_ctx = { path = "../../", editable = true }
