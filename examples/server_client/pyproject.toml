[project]
name = "server-client-example"
version = "0.1.0"
description = "AtomCtx Server-Client example"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "atom_ctx>=0.1.6",
    "rich>=13.0.0",
]

[tool.uv.sources]
atom_ctx = { path = "../../", editable = true }
