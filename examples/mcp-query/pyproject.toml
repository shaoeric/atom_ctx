[project]
name = "atom_ctx-mcp"
version = "0.1.0"
description = "AtomCtx MCP Server - RAG query capabilities via Model Context Protocol"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "atom_ctx>=0.1.6",
    "mcp>=1.8.0",
]
