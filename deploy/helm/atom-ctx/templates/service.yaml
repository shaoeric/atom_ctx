apiVersion: v1
kind: Service
metadata:
  name: {{ include "atom_ctx.fullname" . }}
  labels:
    {{- include "atom_ctx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atom_ctx.selectorLabels" . | nindent 4 }}
